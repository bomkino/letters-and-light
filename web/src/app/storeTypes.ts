/** Shared store type re-exports (kept separate so store.tsx stays a component module). */

export type { AnalysisOutput } from "../image/messages";
export type { ColourRun } from "../wiring/engines";
